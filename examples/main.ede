import json

let fib = func(num) {
    if (num == 0 || num == 1){
        return num
    }
    return fib(num-1) + fib(num-2)
}

let str = match (json.parse("{foo:bar")) {
    case error: println("ignoring error")
}

println(fib(10), "= 55")